/* On charge execute ici une closure afin 
 * d'eviter les colisions possibles entre les diff�rents script
 */
(function($){
	
	/**
	 * On lance des functions lorsque la page est pr�te
	 **/
	$(document).ready(function(){
		// Do some stuff
	});
	
})(jQuery);